/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['aws-devops-practice01.s3.amazonaws.com'], // Replace with your actual S3 bucket domain
    },
  };
  
  module.exports = nextConfig;
  